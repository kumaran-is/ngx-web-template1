/* tslint:disable:no-unused-variable */


describe('Directive: OneTime', () => {
  it('should create an instance', () => {
    // const directive = new OneTimeDirective( 'test', 2, 3);
    const directive = 'test';
    expect(directive).toBeTruthy();
  });
});
